<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fevrec.uok">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" ></uses-permission>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"></uses-permission>

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme"
        >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Mes_Evenement"
            android:label="@string/title_activity_mes_evenement"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".Profils"
            android:label="UOK"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".EventView"
            android:label="@string/title_activity_event_view"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".CreateEvent"
            android:label="@string/title_activity_create_event"
            android:theme="@style/MyMaterialTheme.NoActionBar"></activity>
        <activity android:name=".tools.ContactPickerMulti"
            android:label="@string/title_activity_choose_contact"
            android:theme="@style/MyMaterialTheme" />

    </application>

</manifest>